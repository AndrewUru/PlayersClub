---
import BaseLayout from "../layouts/BaseLayout.astro";

const title = "Leonardo de la Fuente - Contact";
---

<BaseLayout title={title}>
  <div class="content content--page">
    <h1 class="contact-title">Contact</h1>
    <p class="contact-subtitle">
      ¿Tienes alguna pregunta o quieres colaborar en un proyecto fotográfico?
      Completa el formulario y me pondré en contacto contigo lo antes posible.
    </p>

    <form class="contact-form">
      <label for="name" class="form-label">Nombre</label>
      <input
        id="name"
        name="name"
        type="text"
        class="form-input"
        placeholder="Tu nombre"
        required
      />

      <label for="email" class="form-label">Email</label>
      <input
        id="email"
        name="email"
        type="email"
        class="form-input"
        placeholder="Tu email"
        required
      />

      <label for="message" class="form-label">Mensaje</label>
      <textarea
        id="message"
        name="message"
        class="form-textarea"
        rows="6"
        placeholder="Escribe tu mensaje..."
        required
      ></textarea>

      <button type="submit" class="btn-submit">Enviar</button>
    </form>

    <section class="contact-details">
      <p>También puedes escribirme directamente a: 
        <strong>leo.delafuente@example.com</strong>
      </p>
      <p>O llamarme al: <strong>+34 600 00 00 00</strong></p>
    </section>
  </div>

  <style>
    .content {
      max-width: 600px;
      margin: 2rem auto;
      padding: 0 1rem;
    }

    .contact-title {
      text-align: center;
      font-size: 2rem;
      margin-bottom: 0.5rem;
    }

    .contact-subtitle {
      text-align: center;
      margin-bottom: 2rem;
      color: #555;
    }

    .contact-form {
      display: flex;
      flex-direction: column;
    }

    .form-label {
      margin-bottom: 0.5rem;
      font-weight: 600;
    }

    .form-input,
    .form-textarea {
      padding: 0.75rem;
      border: 1px solid #ddd;
      border-radius: 4px;
      margin-bottom: 1.5rem;
      font-size: 1rem;
      width: 100%;
      box-sizing: border-box;
    }

    .form-input:focus,
    .form-textarea:focus {
      outline: none;
      border-color: #0070f3;
    }

    .btn-submit {
      background-color: #0070f3;
      color: #fff;
      padding: 0.75rem 1.5rem;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 1rem;
      align-self: flex-start;
      transition: background-color 0.3s ease;
    }

    .btn-submit:hover {
      background-color: #0059c2;
    }

    .contact-details {
      margin-top: 2rem;
      font-size: 0.95rem;
      color: #333;
      line-height: 1.5;
    }

    .contact-details strong {
      color: #000;
    }
  </style>
</BaseLayout>
